## Gite reposityory

https://github.com/onurikcu/dataops-week15

# GitHub'a gönderme
git add -A
git commit -m "Klasör yapısı VS Code ile aynı olacak şekilde kök dizine alındı"

git push -u origin main

# Docker ayağa kaldır

docker compose up -d


# airflowa giriş(şifre sorgulama komutu)

docker compose logs airflow-webserver | grep -i "password"





